import React from 'react';
import { useParams } from 'react-router-dom';

const ChapterStatusPage: React.FC = () => {
  const { projectId } = useParams<{ projectId: string }>();

  return (
    <div className="h-full bg-cosmic-950 text-white p-6">
      <div className="max-w-4xl mx-auto">
        <div className="mb-8">
          <h1 className="text-3xl font-cosmic text-gold-500 mb-2">
            📊 章節管理
          </h1>
          <p className="text-gray-300">
            管理專案章節的狀態和進度
          </p>
        </div>

        <div className="bg-cosmic-800 rounded-lg p-6 mb-6">
          <h2 className="text-xl text-gold-400 mb-4">✅ 路由測試成功！</h2>
          
          <div className="grid grid-cols-1 md:grid-cols-2 gap-4 text-sm">
            <div>
              <span className="text-gray-400">專案 ID:</span>
              <span className="ml-2 text-white font-mono">{projectId}</span>
            </div>
            <div>
              <span className="text-gray-400">路徑:</span>
              <span className="ml-2 text-white font-mono">{window.location.pathname}</span>
            </div>
            <div>
              <span className="text-gray-400">時間:</span>
              <span className="ml-2 text-white">{new Date().toLocaleString()}</span>
            </div>
            <div>
              <span className="text-gray-400">狀態:</span>
              <span className="ml-2 text-green-400">正常運行</span>
            </div>
          </div>
        </div>

        <div className="bg-cosmic-700 rounded-lg p-6">
          <h3 className="text-lg text-blue-400 mb-3">🚧 開發說明</h3>
          <p className="text-gray-300 mb-4">
            章節管理功能的完整實現需要重新整合章節狀態管理系統。
            目前這是一個基礎的路由測試頁面。
          </p>
          
          <div className="space-y-2 text-sm text-gray-400">
            <p>📋 待實現功能：</p>
            <ul className="list-disc list-inside ml-4 space-y-1">
              <li>章節狀態追蹤 (草稿、寫作中、審核中、完成)</li>
              <li>進度統計和視覺化圖表</li>
              <li>批量操作和狀態管理</li>
              <li>章節分析和建議</li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ChapterStatusPage;